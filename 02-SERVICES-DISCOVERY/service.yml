apiVersion: v1
kind: Service

metadata:
  name: sample-python-service

spec:
  selector:
    app: sample-python-app

  type: LoadBalancer

  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
      nodePort: 30080
